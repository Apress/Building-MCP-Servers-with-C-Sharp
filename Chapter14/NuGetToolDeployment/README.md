# MCP Weather Server (.NET Tool)

A sample MCP server distributed as a .NET global tool.

## Install

```bash
dotnet tool install --global McpWeatherServer
```

## Run

```bash
mcp-weather
```

## Or run without installing (requires .NET 10)

```bash
dnx McpWeatherServer
```
